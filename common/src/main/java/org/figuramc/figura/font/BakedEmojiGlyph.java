package org.figuramc.figura.font;

import net.minecraft.client.gui.font.GlyphRenderTypes;
import net.minecraft.client.gui.font.glyphs.BakedGlyph;

public class BakedEmojiGlyph extends BakedGlyph {
    public BakedEmojiGlyph(GlyphRenderTypes glyphRenderTypes, float f, float g, float h, float i, float j, float k, float l, float m) {
        super(glyphRenderTypes, f, g, h, i, j, k, l, m);
    }
}
